= render :partial => "accounting/select"

#latest_vouchers.float
  - if @vouchers
    %h4 Senaste verifikaten
    = render :partial=>"vouchers/voucher_list", :object => @vouchers, :locals=>{:sort=>false}
  - else
    %strong Du har inte rättigheter att visa den valda serien

#api_voucher.float
  %h4 Verifikat att godkänna
  = render :partial=>"vouchers/voucher_list", :object => @incomplete_vouchers, :locals=>{:sort=>false, :incomplete=>true}

