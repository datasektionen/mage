%h3= @user

%dl
  %dt Har åtkomst?
  %dd= @user.has_access? ? "Ja" : "Nej"
  %dt Är admin?
  %dd= @user.admin? ? "Ja" : "Nej"
  %dt Email
  %dd= link_to(@user.email, "mailto:#{@user.email}")
  %dt Initialer
  %dd= @user.initials
  %dt Standardserie
  %dd= @user.default_series.nil? ? "Ingen" : @user.default_series

%hr

%h4= t('rights')

%table
  %thead
    %tr
      %th Serie
      %th= t 'granted_by'
  %tbody
    - @user.accesses.each do |a|
      %tr
        %td= a.series
        %td= a.granted_by

%p= link_to "Redigera", edit_user_path(@user)
