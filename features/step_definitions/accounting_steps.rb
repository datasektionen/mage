Then /^I should see vouchers for (\d+)$/ do |year|
  #@activity_year = ActivityYear.find_by_year(year)
  pending
end

Then /^I should not see vouchers for (\d+)$/ do |year|
  #@activity_year = ActivityYear.find_by_year(year)
  pending
end
